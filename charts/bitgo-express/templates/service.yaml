apiVersion: v1
kind: Service
metadata:
  name: {{ include "zapbitgo.fullname" . }}
  labels:
    {{- include "zapbitgo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zapbitgo.selectorLabels" . | nindent 4 }}
